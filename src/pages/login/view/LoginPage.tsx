import { Login } from '@src/widgets/login/view/Login';
import type { FC } from 'react';

export const LoginPage: FC = () => {
  return (
    <div>
      <h1>Вход</h1>
      <Login />
    </div>
  );
};
